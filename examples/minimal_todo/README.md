# Minimal Example: Todo API

This folder demonstrates expected generated output:
- `app.py` with `/health` and todo endpoints
- tests executable with `pytest`
- `intent_manifest.json` for traceability
